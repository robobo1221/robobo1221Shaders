float pow2(float x){
    return x*x;
}
float pow3(float x){
    return x*x*x;
}
float pow4(float x){
    return pow2(pow2(x));
}
float pow5(float x){
    return pow4(x)*x;
}
float pow6(float x){
    return pow3(pow2(x));
}
float pow7(float x){
    return pow3(pow2(x))*x;
}
float pow8(float x){
    return pow2(pow4(x));
}

vec2 pow2(vec2 x){
    return x*x;
}
vec2 pow3(vec2 x){
    return x*x*x;
}
vec2 pow4(vec2 x){
    return pow2(pow2(x));
}
vec2 pow5(vec2 x){
    return pow4(x)*x;
}
vec2 pow6(vec2 x){
    return pow3(pow2(x));
}
vec2 pow7(vec2 x){
    return pow3(pow2(x))*x;
}
vec2 pow8(vec2 x){
    return pow2(pow4(x));
}

vec3 pow2(vec3 x){
    return x*x;
}
vec3 pow3(vec3 x){
    return x*x*x;
}
vec3 pow4(vec3 x){
    return pow2(pow2(x));
}
vec3 pow5(vec3 x){
    return pow4(x)*x;
}
vec3 pow6(vec3 x){
    return pow3(pow2(x));
}
vec3 pow7(vec3 x){
    return pow3(pow2(x))*x;
}
vec3 pow8(vec3 x){
    return pow2(pow4(x));
}

vec4 pow2(vec4 x){
    return x*x;
}
vec4 pow3(vec4 x){
    return x*x*x;
}
vec4 pow4(vec4 x){
    return pow2(pow2(x));
}
vec4 pow5(vec4 x){
    return pow4(x)*x;
}
vec4 pow6(vec4 x){
    return pow3(pow2(x));
}
vec4 pow7(vec4 x){
    return pow3(pow2(x))*x;
}
vec4 pow8(vec4 x){
    return pow2(pow4(x));
}